import { getAllExpensesPresentation } from "./presentation/getAllExpenses.presentation";

export const expenseRoutes = {
  getAll: getAllExpensesPresentation,
  // Agrega aquí el resto de handlers CRUD si es necesario
};
